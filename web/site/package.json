{
  "name": "bughouse.app",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "serve": "^11.3.0"
  },
  "scripts": {
    "deploy": "gcloud app deploy app.yaml",
    "build": "cd react-app && yarn build",
    "start": "serve -s react-app/build"
  }
}
