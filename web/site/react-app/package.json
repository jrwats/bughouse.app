{
  "name": "app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@material-ui/core": "^4.9.11",
    "@material-ui/icons": "^4.9.1",
    "@reach/router": "^1.3.4",
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.3.2",
    "@testing-library/user-event": "^7.1.2",
    "chessground": "^7.9.1",
    "firebase": "^7.14.1",
    "history": "^5.0.0",
    "invariant": "^2.2.4",
    "react": "^16.13.1",
    "react-chessground": "^1.5.0",
    "react-dom": "^16.13.1",
    "react-draggable": "^4.4.3",
    "react-firebaseui": "^4.1.0",
    "react-scripts": "3.4.1",
    "socket.io-client": "^2.3.0"
  },
  "engines": {
    "node": "12.x.x"
  },
  "scripts": {
    "start": "PORT=8080 react-scripts start",
    "start_https": "PORT=8080 HTTPS=true SSL_CRT_FILE=localhost.crt SSL_KEY_FILE=localhost.key react-scripts start",
    "build": "react-scripts build",
    "build:dev": "env-cmd -f .env.dev react-scripts build",
    "start:dev": "PORT=8080 serve --cors --ssl-cert localhost.crt --ssl-key localhost.key -s build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "env-cmd": "^10.1.0",
    "serve": "^11.3.2"
  }
}
